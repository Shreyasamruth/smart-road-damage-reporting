{
    "version": 2,
    "buildCommand": "mkdir -p deploy && cd smart-road-damage-reporting-main/frontend && npm install && npm run build && cp -r dist/* ../../deploy/ && cd ../../ && cp -r api deploy/",
    "outputDirectory": "deploy",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        },
        {
            "source": "/uploads/(.*)",
            "destination": "/api/index.py"
        },
        {
            "source": "/((?!api|uploads).*)",
            "destination": "/index.html"
        }
    ]
}